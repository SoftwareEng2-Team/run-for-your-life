name: Run Tests on Push & PR

on:  
  push:
    branches: [main, gamedev] 
  pull_request:
    branches: [main, gamedev]  

jobs:
  test:
    runs-on: ubuntu-latest 

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3  

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20  

      - name: Install dependencies
        run: |
          cd gamedev
          npm install  

      - name: Wait for PostgreSQL to be ready
        run: |
          echo "Waiting for PostgreSQL"
          sleep 10
        
      - name: Initialize Database Schema
        env:
          PGPASSWORD: ${{ secrets.POSTGRES_PASSWORD }}  
          DATABASE_URL: ${{ secrets.DATABASE_URL }}  
        run: |
          export PGPASSWORD=${{ secrets.POSTGRES_PASSWORD }}  # ✅ Ensure password is passed
          psql -h dpg-cusf9fl6l47c7386ejjg-a.oregon-postgres.render.com -U run_for_your_life_user -d run_for_your_life -f database/query.sql 

      - name: Run Tests
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          PGPASSWORD: ${{ secrets.POSTGRES_PASSWORD }}  
        run: |
          cd gamedev
          npm test  
